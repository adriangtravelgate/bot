Para instalar el bot hay que crear un entorno y activar los scripts

python -m venv venv

source venv/bin/activate     (Linux/macOS)
venv\Scripts\activate        (Windows)

Instalar librerias

pip install discord.py apscheduler python-dotenv

En el archivo requirements.txt estan las dependencias necesarias para que funcione el bot